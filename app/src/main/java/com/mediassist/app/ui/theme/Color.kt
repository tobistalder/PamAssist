package com.mediassist.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary: Teal ────────────────────────────────────────────────────────────
val TealPrimary       = Color(0xFF4BBDBC)
val TealDark          = Color(0xFF1D7070)
val TealLight         = Color(0xFFE4F4F3)
val TealContainer     = Color(0xFFB2DFDB)

// ─── Secondary: Sage Green ────────────────────────────────────────────────────
val SagePrimary       = Color(0xFF8FA870)
val SageDark          = Color(0xFF617A49)
val SageLight         = Color(0xFFEBF1E3)

// ─── Backgrounds & Surfaces ───────────────────────────────────────────────────
val Background        = Color(0xFFFAF2E1)   // fondo principal — warm golden cream
val Surface           = Color(0xFFF2E8D0)   // superficies elevadas / bottom sheet
val CardBackground    = Color(0xFFFFFFFF)   // tarjetas
val DividerColor      = Color(0xFFDDD6C4)   // separadores
val BorderColor       = Color(0xFFC8BFA8)   // bordes de inputs y outline

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFF1C2B2B)
val TextSecondary     = Color(0xFF4A6060)
val TextHint          = Color(0xFF8AABAB)
val TextOnPrimary     = Color(0xFFFFFFFF)
val TextOnSage        = Color(0xFFFFFFFF)

// ─── Semantic States ──────────────────────────────────────────────────────────
val ErrorRed          = Color(0xFFE57373)
val ErrorRedLight     = Color(0xFFFFEBEE)
val WarningAmber      = Color(0xFFF9A825)
val WarningAmberLight = Color(0xFFFFF8E1)
val SuccessGreen      = Color(0xFF8FA870)
val SuccessGreenLight = Color(0xFFEBF1E3)
val InfoTeal          = Color(0xFFB2DFDB)

// ─── Overlay / Scrim ──────────────────────────────────────────────────────────
val Scrim             = Color(0x99000000)
